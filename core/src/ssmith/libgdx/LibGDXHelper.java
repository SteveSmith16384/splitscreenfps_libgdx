package ssmith.libgdx;

public class LibGDXHelper {

	public LibGDXHelper() {
	}
	
	/*
	public static MyBoundingBox createFromCentreAndExtents(Vector3 centre, float x, float y, float z) {
		Vector3 min = new Vector3(centre.x-x, centre.y-y, centre.z-z);
		Vector3 max = new Vector3(centre.x+x, centre.y+y, centre.z+z);
		MyBoundingBox bb = new MyBoundingBox(min, max);
		return bb;
	}
*/
}
